package ui.UIInterface;


public class UI_World extends UI{
	/*Display the initial animation if the player hasn't played this game before.
	 *In this class(screen), you may need no buttons.
	 *If the screen is pressed, then skip the animation and enter next screen.*/
	
	@Override
	public void printScreen(){
		//This function may need to pass the screen object to modify it.
	}
}
